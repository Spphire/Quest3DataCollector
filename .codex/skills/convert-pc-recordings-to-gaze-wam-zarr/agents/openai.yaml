interface:
  display_name: "Build Collector Gaze-WAM Zarr"
  short_description: "Filter recording batches into validated Gaze-WAM Zarr"
  default_prompt: "Use $convert-pc-recordings-to-gaze-wam-zarr to filter this Collector recording batch, build its Zarr, and validate the result."
